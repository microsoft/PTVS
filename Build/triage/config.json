{
    "$comment": "Configuration for the weekly AzDO triage automation (read-only fetch + sanitize + summary issue). See Build/triage/README.md.",

    "azdo": {
        "organization": "devdiv",
        "project": "DevDiv",
        "baseUrl": "https://dev.azure.com/devdiv",
        "areaPath": "DevDiv\\Python and AI Tools",
        "$comment_workItemTypes": "Scopes the report to externally-reported items only. 'FeedbackTicket' is the type the VS Developer Community → AzDO sync creates for every imported item, and the only type with the Microsoft.DevDiv.DeveloperCommunityId field populated. Internal-only work (S360, compliance, internal Watson buckets, accessibility benchmarks, etc.) is filed as Bug / Task / Assessment and is excluded by construction. Empty array means 'any work item type'.",
        "workItemTypes": [ "FeedbackTicket" ],
        "$comment_dateField": "Which date field the lookback filters on. 'ChangedDate' (default) catches reopens and recent activity on older items; 'CreatedDate' catches only newly-filed items.",
        "dateField": "ChangedDate",
        "$comment_excludedStates": "WIQL clause uses substring match (NOT [System.State] CONTAINS '<value>'), so 'Closed' also excludes DC close-states like 'DC - Closed - Duplicated'.",
        "excludedStates": [ "Closed", "Resolved", "Completed", "Removed" ],
        "excludedTags": [ "triaged-by-ai", "do-not-triage" ]
    },

    "github": {
        "owner": "microsoft",
        "repo": "PTVS",
        "defaultAssignee": "StellaHuang95",
        "reportLabel": "azdo-triage-report"
    },

    "limits": {
        "maxCandidatesPerRun": 25,
        "lookbackDaysDefault": 7
    }
}
