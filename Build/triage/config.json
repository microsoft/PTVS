{
    "$comment": "Configuration for the weekly AzDO triage automation (read + AI draft only; no destructive AzDO writes). See Build/triage/README.md.",

    "azdo": {
        "organization": "devdiv",
        "project": "DevDiv",
        "baseUrl": "https://dev.azure.com/devdiv",
        "areaPath": "DevDiv\\Python and AI Tools",
        "$comment_workItemTypes": "Empty array means 'any work item type'. Mirrors the manual top-level-items query: Work Item Type = [Any].",
        "workItemTypes": [],
        "$comment_dateField": "Which date field the lookback filters on. 'ChangedDate' (default) catches reopens and recent activity on older items; 'CreatedDate' catches only newly-filed items.",
        "dateField": "ChangedDate",
        "$comment_excludedStates": "WIQL clause uses substring match (NOT [System.State] CONTAINS '<value>'), so 'Closed' also excludes DC close-states like 'DC - Closed - Duplicated'.",
        "excludedStates": [ "Closed", "Resolved", "Completed", "Removed" ],
        "excludedTags": [ "triaged-by-ai", "do-not-triage" ]
    },

    "github": {
        "owner": "microsoft",
        "repo": "PTVS",
        "defaultAssignee": "StellaHuang95",
        "reportLabel": "azdo-triage-report"
    },

    "limits": {
        "maxCandidatesPerRun": 25,
        "lookbackDaysDefault": 7,
        "recentCommitsCap": 150,
        "releaseTagsCap": 3
    },

    "$comment_upstream_trackers": "Hint URLs the AI draft may suggest to customers when their issue looks like an upstream defect rather than PTVS itself.",
    "upstream_tracker_urls": [
        "https://github.com/microsoft/pylance-release/issues",
        "https://github.com/microsoft/debugpy/issues",
        "https://github.com/python/cpython/issues"
    ]
}
