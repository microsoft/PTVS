Activation
- For workspace
- For loose files
- For each project
- For each interactive window

WpfTextViewConnectionListener
SubjectBuffersConnected
- change the content type "python-project1", based on "python"
- only need if need to get the view - find equivalent for text buffer
- LS client will be able to tell them apart since each instance has its own content type
- don't export via MEF, do activation at runtime

Language server lifetime
- Certain settings changes (passed to init options) require restart (diff python interpreter/version)
- Ability to restart after crash
