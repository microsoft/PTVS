.. _api_default:

Core API
========

Although each module is documented separately (:mod:`xl.range`, :mod:`xl.tools`, etc.), the top-level :mod:`xl` package imports the most important pieces. Most users only need to import :mod:`xl`.

:mod:`xl`
---------

.. We disable inclusion of members here, because it would be redundant with the other pages

.. automodule:: xl
	:no-members: